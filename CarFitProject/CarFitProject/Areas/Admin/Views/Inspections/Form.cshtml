@model CarFitProject.ViewModel.InspectionReportFormViewModel
@{
    ViewData["Title"] = SharedLocalizer["Admin.Inspections.Form.Title"].Value;
    var returnAction = (string?)ViewBag.ReturnAction ?? "Index";
    var returnController = (string?)ViewBag.ReturnController ?? "Listings";
    var listingId = (int?)ViewBag.ListingId;
    var isAdminArea = (string?)ViewContext.RouteData.Values["area"] == "Admin";
    if (isAdminArea)
    {
        Layout = "/Areas/Admin/Views/Shared/_Layout.cshtml";
    }
}

<div class="container py-4">
    <a asp-area="@(isAdminArea ? "Admin" : "Seller")" asp-controller="@returnController" asp-action="@returnAction" class="text-decoration-none small fw-bold @(isAdminArea ? "text-primary" : "text-primary")">
        <i class="fa fa-arrow-left me-1"></i>Back
    </a>

    <div class="@(isAdminArea ? "admin-card" : "bg-white shadow-sm rounded-4") p-4 mt-3">
        <div class="d-flex justify-content-between align-items-start mb-3">
            <div>
                <h2 class="fw-bold mb-1 @(isAdminArea ? "text-white" : "text-dark")">Inspection report</h2>
                <p class="mb-0 @(isAdminArea ? "text-muted" : "text-muted")">@Model.CarLabel</p>
            </div>
            @if (Model.OverallScore.HasValue)
            {
                <div class="text-end">
                    <span class="badge bg-primary px-3 py-2 fs-6">جيد @Model.OverallScore.Value.ToString("0.0")</span>
                    <div class="text-muted small mt-1">Trust @Model.CalculatedTrustScore?.ToString("0.00") / 5</div>
                </div>
            }
        </div>

        <form method="post" asp-action="Edit" asp-route-carId="@Model.CarId" novalidate>
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>
            <input type="hidden" asp-for="CarId" />

            <partial name="_InspectionReportFields" model="Model" />

            <div class="d-flex justify-content-end gap-2 mt-4">
                <a asp-area="@(isAdminArea ? "Admin" : "Seller")" asp-controller="@returnController" asp-action="@returnAction"
                   class="btn @(isAdminArea ? "btn-outline-light" : "btn-outline-secondary") fw-bold">Cancel</a>
                <button type="submit" class="btn btn-primary fw-bold"><i class="fa fa-floppy-disk me-1"></i>Save inspection</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
